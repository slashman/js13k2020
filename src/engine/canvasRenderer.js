// src/canvasRenderer.js >>>

var animationIndex = 0;
var pixelSize = 2;
//var frames = animations[0];

function draw() {
  //drawBackground();
  graphics.clearRect(0, 0, 320, 240);
  sceneManager.draw();
}

// ending file
