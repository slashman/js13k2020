
// src/gameObjects/character.js

var Character = props => {
  var base = GameObject(props);
  var self = {

  };
  return extendFunction(base, self);
}

// end of src/gameObjects/character.js